Topics: Math, Bit Manipulation

class Solution {
    
    public boolean isPowerOfTwo(int n) {
        if(n<=0) return false;
        int m = n - 1;
        int ans = n & m;
        if(ans==0) return true;
        return false;
    }
}

Time Complexity: O(1);
Auxiliary Space: O(1);
