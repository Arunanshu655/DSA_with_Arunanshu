Topics: Recursion, Math

class Solution {
    public boolean isPowerOfThree(int n) {
        if(n==1) return true;
        if(n % 3 != 0) return false;
        if(n<=0) return false;

        return isPowerOfThree(n/3);
    }
}

Time Complexity: O(logn);
Auxiliary Space: O(long); // call stack
